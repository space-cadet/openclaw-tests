# Session: 2026-08-15 Night

## T11 — Provider/model availability study harness

### Completed
- Created and pushed the provider/model-agnostic T11 design and task.
- Verified the existing `cron-management` skill and maintenance-mode contract.
- Implemented `scripts/model-availability-batch.py`.
- Enabled `availability-probe-openclaw` as a silent command cron every five minutes, with random 5--20 minute batch spacing.
- Added four Kimi targets and `deepseek/deepseek-v4-flash` reference target.
- Added JSONL logging for latency, token usage, cache usage, stop reason, fallback status, and failure class.
- Verified a full batch: four Kimi successes; DeepSeek reference failure.
- Committed reusable implementation as `bd566ee`; runtime logs remain local.

### Important decisions
- Operational condition uses the OpenClaw harness while the active controller remains OpenAI.
- Direct `probe-ok` smoke calls are excluded from the operational sample.
- Measurement jobs are silent; only future aggregate jobs may notify Telegram.
- T11 remains active until hourly/four-hourly aggregate reporting is implemented.

### Open items
- Diagnose/configure the DeepSeek reference route if it is to become a valid comparison series.
- Implement hourly and four-hourly aggregate reports.

### Follow-up — repository results organization (2026-08-16 01:28 IST)
- Confirmed raw availability data is recorded in `data/model-availability/runs.jsonl`.
- Confirmed older capability benchmark results live under `tests/model-benchmarks/` and `kimi-benchmarks/`.
- Recorded the planned separation into availability/capability aggregate reports; no aggregate implementation was added.
